const titreDialog = document.getElementById("titleModal");
const texteModal = document.getElementById("texteModal");

function setTitreModal(titre) {
    titreDialog.textContent = titre;
}

function setTexteModal(texte) {
    texteModal.textContent = texte;
}